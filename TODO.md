# TODO / بک‌لاگ آرامینا

فهرست کارهای باقی‌مانده و ایده‌ها. هر آیتم یک توضیح کوتاه دارد تا بعداً راحت برداریمش و بزنیم.
`- [ ]` = مانده، `- [x]` = انجام‌شده.

---

## ✅ انجام‌شده در جلسه‌ی اخیر (نوتیفیکیشن push)
- [x] بک‌اند FCM: جدول `device_tokens`، اندپوینت `POST /devices`، ارسال push هنگام پیام روانشناس و پیام خودکار ۸ شب.
- [x] موبایل: گرفتن/ثبت توکن، هندلر foreground/background، صندوق پیام (`MessagesScreen`)، badge زنگوله.
- [x] کانال نوتیفیکیشن `aramina_default` (بدون آن اندروید ۸+ نوتیف نشان نمی‌دهد).
- [x] بنر درون‌اپ به‌جای Alert، و طراحی زیبای صفحه‌ی پیام‌ها.
- [x] کانال crisis (`aramina_crisis`) با bypass DND + فلگ `urgent` در پنل روانشناس.

---

## 🔧 کارهای باقی‌مانده‌ی همین بخش (Follow-ups)

### نوتیفیکیشن
- [ ] **تپ روی نوتیف → باز شدن مستقیم صندوق پیام.** الان تپ فقط اپ را باز می‌کند. باید با `getInitialNotification` / `onNotificationOpenedApp` به صفحه‌ی «پیام‌های همراهی» برود.
- [ ] **درخواست دسترسی DND برای crisis.** یک پیام/دکمه در اپ که کاربر را به تنظیمات «Do Not Disturb access» ببرد؛ بدون آن، crisis سکوت را نمی‌شکند.
- [ ] **راهنمای battery-optimization** برای گوشی‌های شیائومی/هواوی/سامسونگ که ممکن است FCM را در پس‌زمینه بکُشند.
- [ ] **iOS / APNs** هنوز ست نشده — فعلاً فقط اندروید. نیازمند Apple Developer Account + کلید APNs در Firebase.

### بک‌اند
- [ ] **ذخیره‌ی فلگ `urgent` در دیتابیس** (ستون جدید) تا در صندوق پیام هم برچسب «فوری 🚨» نشان داده شود (الان urgent فقط روی push اثر دارد، نه متن ذخیره‌شده).
- [ ] **پاک‌سازی داده‌ی تستی قبل از پروداکشن:** اکانت «دکتر تست» (۰۹۱۵۰۰۰۹۰۹۰) و پیام‌های تستی.

### فرانت‌اند وب (پنل روانشناس)
- [ ] **چک‌باکس «فوری»** کنار فرم ارسال پیام تا روانشناس بتواند `urgent:true` بفرستد (الان فقط با API قابل تست است).

### موبایل (UX)
- [ ] **تاریخ شمسی (جلالی)** در صندوق پیام به‌جای میلادی.
- [ ] **persist کردن تاگل «اعلان‌ها»** در Settings (الان فقط `useState` است و ذخیره نمی‌شود).
- [ ] **pull-to-refresh** در صندوق پیام.

---

## 🆕 کارهای اضافه‌ی من (اینجا بنویس)
> هرچه می‌خواهی را اینجا با یک خط توضیح اضافه کن؛ بعد با هم برمی‌داریم و می‌زنیم.

- [ ] …
- [ ] …
- [ ] …
