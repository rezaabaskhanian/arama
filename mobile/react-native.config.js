/**
 * Bundles custom assets (the Vazir font) into the native iOS/Android projects.
 * After changing this, run `npx react-native-asset` and rebuild the app so the
 * font family "Vazir" becomes available to `fontFamily: 'Vazir'`.
 */
module.exports = {
  assets: ['./src/assets/fonts'],
};
